# Bob · Level 100 · Begin — the worksheet, started

*Practitioner #1. Drafted 11 September 2026 from Bob's Wren Card; steps 1–4 and 6 are pre-filled in
Bob's own terms for him to strike or keep. Step 5 is his ten minutes in a browser — nobody else can
make his key. Thursday 17 September, 15:00 Pacific; Ken marks.*

**Scenario:** VH-001, Air Canada. **Lens:** engineer; Wren picked **regulator** as the lens not his own.
**Path:** both. **Show it to:** Ken.

### 1 · Read the case — what the AI did that mattered

> It made a promise to a customer, about policy, with no source behind it — and the company was held to
> the promise.

### 2 · Name the consequential action

- [x] **C2** external effect on a person, reversible — `customer.reply` carrying a policy claim.
  (Omni's nearest cousin: `welcomer.draft_post` and the cohort welcome — a promise to a member.)

### 3 · Declare the gate

**Plain words (Bob's rule):**

> An agent may answer a question about policy only when the answer carries a verified source; without
> one it says it does not know and routes to a person. No maybe.

**Code (`policy.yaml`, unchanged):**

```yaml
policy: begin-v1
controls:
  - action: customer.reply
    tier: C2
    decision: constrain      # allowed only with a verified policy source attached
```

Check: the sentence and the YAML say the same thing — *constrain* means "with the source attached", and
"routes to a person" is what the constraint falls back to. ✓

### 4 · Decide

Proposed action: the bot is about to tell a customer they can claim a bereavement fare after travel.

| allow | **constrain** | hold | deny |
|---|---|---|---|

**Constraint applied:** reply only with the bereavement-fare policy page attached as the source; if the
claim is not on that page, do not make it — hand off.

### 5 · Prove — Bob's ten minutes (not draftable)

Open **[Beacon in worksheet mode](https://aigovops-foundation.github.io/aigovops-beacon/?worksheet=100)** — one button; the three values appear on screen:

- Key fingerprint: __________________________
- Bundle hash: __________________________
- Number of receipts: ______

### 6 · Read it in the regulator's lens

*What does this bundle prove happened, and what does it not?*

> It proves that these decisions were signed by this key, in this order, against policy `begin-v1`, and
> that none was removed. It does not prove what the customer was told, and it never carries that — a
> regulator gets the chain, not the conversation.

### 7 · Return

Bundle hash on the Gate Card → Thursday 17 September 15:00 Pacific → show it to Ken and say the rule in step 3
→ Ken writes the line:

`MARK · 100 · Bob · <bundle hash, first 12> · host Ken · 2026-09-17`

---

*Wren's last line:* the only step left is the one only you can do. Ten minutes in a browser, then it's
Thursday. Out of how many? One.
